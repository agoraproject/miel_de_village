{%extends "::layout.html.twig"%}

{% block window -%}
    <h1>MotifDeplacement</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ entity.libelle }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('motifdeplacement') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('motifdeplacement_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
  
</ul>
{% endblock %}
