{%extends "::layout.html.twig"%}

{% block window  -%}
    <h3>Introduction</h3>

    {{ form(edit_form) }}

     <ul class="record_actions">
        <li>
            <a href="{{ path('introduction') }}">
                <a href="{{path('introduction_delete', {'id': entity.id})}}" class="btn btn-default">Supprimer</a>
                <a href="{{path('introduction')}}" class="btn btn-default">Retour à la liste</a>
        </li>
    </ul>
{% endblock %}
