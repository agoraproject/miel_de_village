{%extends "::layout.html.twig"%}

{% block window  -%}
    <h1>ActionListe</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ entity.libelle }}</td>
            </tr>
            <tr>
                <th>Codeaction</th>
                <td>{{ entity.codeAction }}</td>
            </tr>
            <tr>
                <th>Qteaffichage</th>
                <td>{{ entity.qteAffichage }}</td>
            </tr>
            <tr>
                <th>Uniteqte</th>
                <td>{{ entity.uniteQte }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('actionliste') }}">
            Retour à la liste
        </a>
    </li>
    <li>
        <a href="{{ path('actionliste_edit', { 'id': entity.id }) }}">
            Editer
        </a>
    </li>
</ul>
{% endblock %}
