{%extends "::layout.html.twig"%}

{% block window -%}
    <h3>ChangementStatut</h3>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ entity.date|date('d/m/Y') }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('changementstatut') }}">
            Retour à la liste
        </a>
    </li>
    <li>
        <a href="{{ path('changementstatut_edit', { 'id': entity.id }) }}">
            Editer
        </a>
    </li>
   
</ul>
{% endblock %}
