{%extends "::layout.html.twig"%}

{% block window  -%}
    <h1>Statut de la reine</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ entity.libelle }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('statutreine') }}">
            Retour à la liste
        </a>
    </li>
    <li>
        <a href="{{ path('statutreine_edit', { 'id': entity.id }) }}">
            Editer
        </a>
    </li>
</ul>
{% endblock %}
