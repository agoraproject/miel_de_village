{%extends "::layout.html.twig"%}

{% block window  -%}
    <h3>Type de récolte</h3>

    <table class="records_list">
        <thead>
            <tr>
                <th>Type de récolte</th>
               
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td>{{ entity.libelle }}</td>
              
                <td> <a href="{{path('typerecolte_edit', {'id': entity.id})}}" >Editer</a></td>
                <td> <a href="{{path('typerecolte_delete', {'id': entity.id})}}" >Supprimer</a></td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('typerecolte_new') }}">
                Ajouter
            </a>
        </li>
    </ul>
    {% endblock %}
