{%extends "::layout.html.twig"%}

{% block window  -%}
    <h1>Eleveur</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ entity.nom }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('eleveur') }}">
            Retour à la liste
        </a>
    </li>
    <li>
        <a href="{{ path('eleveur_edit', { 'id': entity.id }) }}">
            Editer
        </a>
    </li>
   
</ul>
{% endblock %}
