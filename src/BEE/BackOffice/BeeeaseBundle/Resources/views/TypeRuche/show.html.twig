{%extends "::layout.html.twig"%}

{% block window  -%}
    <h1>TypeRuche</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Libelletyperuche</th>
                <td>{{ entity.libelletyperuche }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('typeruche') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('typeruche_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
