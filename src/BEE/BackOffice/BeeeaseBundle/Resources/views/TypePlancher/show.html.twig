{%extends "::layout.html.twig"%}

{% block window  -%}
    <h3>TypePlancher</h3>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ entity.libelle }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('typeplancher') }}">
            Retour à la liste
        </a>
    </li>
    <li>
        <a href="{{ path('typeplancher_edit', { 'id': entity.id }) }}">
            Editer
        </a>
    </li>
   
</ul>
{% endblock %}
